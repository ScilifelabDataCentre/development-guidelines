# Linting with Prettier

This repo uses [Prettier](https://prettier.io/) to format the markdown correctly.

Install the correct version of Prettier via npm:
`npm ci`

Run prettier locally:
`npx prettier --check '**/*.md'`
`npx prettier --write '**/*.md'`
